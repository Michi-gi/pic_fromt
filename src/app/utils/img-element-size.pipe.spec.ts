import { ImgElementSizePipe } from './img-element-size.pipe';

describe('ImgElementSizePipe', () => {
  it('create an instance', () => {
    const pipe = new ImgElementSizePipe();
    expect(pipe).toBeTruthy();
  });
});
